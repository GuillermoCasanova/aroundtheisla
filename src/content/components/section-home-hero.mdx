---
title: SectionHomeHero
description: Homepage frame — one photograph picked at random from the Sanity hero set (up to 5) on each landing.
category: pattern
order: 10
sourceFile: src/components/SectionHomeHero.astro
status: per-project
related: [HeroBanner, SectionHomeSlideshow, NavInline]
---

import PropsTable from "@components/_docs/PropsTable.astro";

Figma Home (`222:21`): a white canvas and one photograph, centered. The shot is chosen at random from the homepage's Sanity hero photographs (up to 5) whenever the page loads. Brand + menu stay in `NavInline`.

## Props

<PropsTable props={[
  { name: "images", type: "CmsImage[]", required: true, description: "Hero photographs from Sanity. One is shown at random." },
  { name: "class", type: "string", description: "Extra classes on the section root." },
]} />

## Layout

- Full-viewport on purpose — does not wrap `SectionMain`, so the crop matches the edge-to-edge Figma composition.
- Images are authored on the Home document in Studio (`Hero photographs`).
- Without JavaScript the first photograph is shown.
